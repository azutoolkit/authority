# Entity alias for User model
# This file provides backward compatibility
module Authority
  # User entity is now defined in src/models/user.cr
  # This file exists for backward compatibility with existing imports
end
